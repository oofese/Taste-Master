@page
@model Taste.Pages.Admin.Order.OrderListModel
@{
    ViewData["Title"] = "OrderList";
}

@{
    var status = HttpContext.Request.Query["status"];
    var cancelled = "text-primary";
    var completed = "text-primary";
    var inProcess = "text-primary";

    if (status == "cancelled")
    {
        cancelled = "active text-white";
    }
    else
    {
        if (status == "completed")
        {
            completed = "active text-white";
        }
        else
        {
            inProcess = "active text-white";
        }
    }

}


<div class="border backgroundWhite container">
    <div class="d-flex justify-content-between mb-3">
        <div class="p-2">
            <h2 class="text-primary">Sipariş Listesi</h2>
        </div>
        <div class="p-2">
            <ul class="list-group list-group-horizontal-sm">
                <a style="text-decoration:none;" asp-page="OrderList" asp-route-status="cancelled">
                    <li class="list-group-item @cancelled"> İptal edildi </li>
                </a>
                <a style="text-decoration:none;" asp-page="OrderList" asp-route-status="completed">
                    <li class="list-group-item @completed"> Tamamlandı </li>
                </a>
                <a style="text-decoration:none;" asp-page="OrderList" asp-route-status="inProcess">
                    <li class="list-group-item @inProcess"> İşlem sırasında </li>
                </a>
            </ul>
        </div>
    </div>
    <br /><br />
    <table id="DT_load" class="table table-striped table-bordered" style="width:100%">
        <thead>
            <tr>
                <th>Sipariş ID</th>
                <th>Adı</th>

                <th>Email</th>
                <th>Toplam fiyatı</th>
                <th></th>
            </tr>
        </thead>
    </table>
</div>

@section Scripts{
    <script src="~/js/orderList.js"></script>
}

